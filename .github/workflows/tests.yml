name: tests

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * 0"

jobs:
  test:
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == 'totallynotdavid' }}

    environment:
      name: testing

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.11"

      - run: bun install --frozen-lockfile

      - run: bun test --coverage --coverage-reporter=lcov
        env:
          BING_COOKIE: ${{ secrets.BING_COOKIE }}
        timeout-minutes: 5

      - uses: codecov/codecov-action@57e3a136b779b570ffcdbf80b3bdc90e7fab3de2 #v6.6.0
        with:
          files: ./coverage/lcov.info

      - if: failure()
        run: |
          echo "::error::Tests failed. The Bing API may have changed or the cookie may have expired."
